namespace ProjectPower.WorkoutCreator
{
    class CreateWorkout
    {
        //private List<Exercise> workoutExerciseList;

        void CreateExercise(decimal weight, int reps, string name)
        {
            //Exercise exercise = new Exercise(weight,reps,name);
            //workoutExerciseList.Add(exercise);
            //WorkoutExerciseListFactory factory = new WorkoutExerciseListFactory(workoutExerciseList);
            //factory.CreateExerciseList();
        }
        
    }
}
